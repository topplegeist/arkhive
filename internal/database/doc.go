/*
The database handle and protects the whole application data.

On launch it imports the database from the supported files and synchronize them with the
delegated database management system.
*/
package database
