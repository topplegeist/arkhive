/*
Package launcher implements the business intellingence of the arkHive launcher application.

The project has three main source packages:
`cmd`: Main applications, tools and libraries.
`internal`: Private application and library code.
`pkg`: Library code that's ok to use by external applications

Apart from these folder the project is composed by a `test` folder that keeps the assets used
to test the internal packages.
*/
package launcher
